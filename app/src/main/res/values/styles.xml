<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DetailHeaders" parent="AppTheme">
        <item name="android:paddingTop">@dimen/detail_category_padding</item>
        <item name="android:paddingBottom">@dimen/detail_category_padding</item>
        <item name="android:layout_marginBottom">@dimen/detail_item_margin</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">?colorPrimary</item>
    </style>

    <style name="DetailButton" parent="AppTheme">
        <item name="android:layout_marginBottom">@dimen/detail_button_margin</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">?colorPrimary</item>
    </style>

</resources>
