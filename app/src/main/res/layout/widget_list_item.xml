<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_list_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/touch_selector_white"
    android:minHeight="?android:attr/listPreferredItemHeight"
    android:paddingLeft="@dimen/abc_list_item_padding_horizontal_material"
    android:paddingRight="@dimen/abc_list_item_padding_horizontal_material"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:paddingLeft="@dimen/widget_title_padding"
                android:paddingStart="@dimen/widget_title_padding"
                android:paddingRight="@dimen/widget_title_padding"
                android:paddingEnd="@dimen/widget_title_padding"
                android:fontFamily="sans-serif-light"
                android:textColor="#B71C1C"
                android:textSize="@dimen/widget_title_size"
                tools:text = "Doctor Strange (2015) #11"
                />

            <TextView
                android:id="@+id/widget_issue_number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-light"
                android:textColor="@color/colorText"
                android:gravity="center_horizontal"
                android:textSize="@dimen/widget_subtext_size"
                tools:text = "Issue #: 11"
                />

            <TextView
                android:id="@+id/widget_pages"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-light"
                android:textColor="@color/colorText"
                android:gravity="center_horizontal"
                android:textSize="@dimen/widget_subtext_size"
                tools:text = "15 Pages"
                />

            <TextView
                android:id="@+id/widget_price"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-light"
                android:textColor="@color/colorText"
                android:gravity="center_horizontal"
                android:textSize="@dimen/widget_subtext_size"
                tools:text = "$2.99"
                />

        </LinearLayout>
    </FrameLayout>